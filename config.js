var config = {server: {}, database: {}};

config.server.port = 8080;

config.database.username = 'root';
config.database.password = '';
config.database.hostname = 'localhost';
config.database.name = 'restaurant';

module.exports = config;